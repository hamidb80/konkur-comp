[
(p 
  `کدام مورد نادرست است؟`)

(ol 
  (sp
    `اگر جدولی` _ 
    `3NF` _ 
    `باشد، حتما` _ 
    `BCNF` _ 
    `هم هست`
    )
  (sp 
    `اگر جدولی` _ 
    `3NF` _ 
    `باشد، حتما` _ 
    `2NF` _ 
    `هم هست`
    )
  (sp 
    `ممکن است یک جدول` _ 
    `3NF` _
    `باشد ولی شامل وابستگی های غیرکلیدی باشد` 
    )
  (sp 
    `3NF` _ 
    `بودن، تضمینی برای حفظ وابستگی تابعی` _ 
    `Functional Dependency` _ 
    `است`
    )
  )
]