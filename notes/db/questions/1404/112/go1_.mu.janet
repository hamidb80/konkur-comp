[
  (ltx `
  f = \{A\}                \\
  f^* \to \{A, B, C\}       \\
     \to \{A, B, C, D\}    \\
     \to \{A, B, C, D, E\} \\
  `)

(p 
  `با` _ 
  (ltxi `A`) _ 
  `توانستیم همه` _ 
  :db/terms/attr
  `های دیگر را` _ 
  `بجر صفت`
  (ltxi `F`)
  `بدست بیاوریم.` _ 
  `پس` _ 
  (ltxi `A`) _ 
  `کلید کاندید نیست`
  )
]