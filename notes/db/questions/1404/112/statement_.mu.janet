[
(p 
  `با توجه به مجموعه وابستگی های تابعی داده شده برای` _ 
  (ltxi `r(A,B,C,D,E,F)`) _ 
  `کدام مورد درست است؟`
  )

(ul
  (ltxi `A  \to BCD`)
  (ltxi `BC \to DE`)
  (ltxi `B  \to D `)
  (ltxi `D  \to A`)
    )

(ol
  (sp 
    (ltxi `A`) _
    `کلید کاندید است`)
  
  (sp 
    (ltxi `AF`) _
    `ابرکلید است`)
  
  (sp 
    (ltxi `B`) _
    `صفت کلیدی است`)

  (sp 
    (ltxi `D`) _
    `صفت کلیدی است`)
  )
]