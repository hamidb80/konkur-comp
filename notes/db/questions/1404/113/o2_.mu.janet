[
(p 
  (u `در گزینه 2`) _
  `در گزینه 2با در نظر گرفتن ستون های` _ 
  (ltxi `A`)
  `و`
  (ltxi `C`)
  `، این اطلاعات را به ازای هر سطر داریم:`
  )

(ol
  (ltxi `a_1 \to c_1`)
  (ltxi `a_1 \to c_2`)
  (ltxi `a_2 \to c_1`))

(p 
  `بخاطر وجود سطر 1 و 2 وابستگی تابعی رعایت نشده`
)
]