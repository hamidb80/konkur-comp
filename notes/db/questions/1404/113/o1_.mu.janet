[
(p 
  (u `در گزینه 1`) _
  `با در نظر گرفتن ستون های` _ 
  (ltxi `A`)
  `و`
  (ltxi `B`)
  `، این اطلاعات را به ازای هر سطر داریم:`
  )

(ol
  (ltxi `a_1 \to b_1`)
  (ltxi `a_1 \to b_1`)
  (ltxi `a_2 \to b_1`))

(p 
  `وابستگی تابعی در اینجا رعایت شده چون به ازای مقدار`
  (ltxi `a`) _ 
  `تکراری، مقادیر` _ 
  (ltxi `b`) _ 
  `نیز تکراری هستند`
)
]