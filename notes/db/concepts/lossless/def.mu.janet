[
(p 
  `تجزیه یک رابطه`
  (ltxi `R`) _
  `به دو رابطه` _
  (ltxi `R_1`) _
  `و` _
  (ltxi `R_2`) _
  `را` _
  (b `بدون گم گشتگی`) _
  `یا` _
  (b `lossless`) _
  `می گوییم هرگاه:`)

(ltx `R_1 \bowtie R_2 = R`)

(p (b `نکته: `) `به علامت مساوی توجه کنید`)
(p (b `نکته: `) 
  `عدم برقراری` _ 
  `lossless join` _ 
  `وضعیت` _
  :db/terms/consistency _ 
  `پایگاه داده را مختل میکند`
  )

(hr)

(ul
  (ref :db/concepts/lossless/decomposition `چطور تجزیه بدون گمشتگی داشته باشیم`))
]