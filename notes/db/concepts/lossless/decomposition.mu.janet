[
(p 
  `شرط` _
  `lossless` _
  `بودن یک تجزیه این است که حداقل یکی از دو حالت زیر برقرار باشد:`)

(ul
  (ltxi `R_1 \cap R_2 \to R_1`)
  (ltxi `R_1 \cap R_2 \to R_2`))

(p 
  `یا با عبارتی، اشتراک دو رابطه تجزیه شده،` _
  (ref :db/concepts/keys/super-key `ابر کلید`) _
  `حداقل یکی از دو رابطه باشد`)
]