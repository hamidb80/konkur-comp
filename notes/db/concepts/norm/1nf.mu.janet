[
(p 
  (b `نرمال سازی سطح اول`) _ 
  `یا` _ 
  (b `1NF`) _ 
  `(first normal form)` _ 
  `اولین مرحله برای تبدیل داده منطقی به فرم رابطه است` _ 
  `که بیان میکند:`
  )

(p 
  :db/terms/attr
  `های یک` _
  :db/terms/relation _ 
  `باید به صورت` _
  `ساده` _ 
  `(atomic)` _ 
  `باشند.` _ 
  `به عبارتی،` _ 
  `تو در تو` _ 
  `(nested)` _ 
  `نباشند` _ 
  )
]