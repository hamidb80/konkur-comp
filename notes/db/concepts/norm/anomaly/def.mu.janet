[
(p
  (b `بد رفتاری داده`)  _ 
  `یا` _ 
  (b `data anomaly`) _
  `به مشکلاتی گفته میشه که باعث و بانی اون` _
  (b `ساختار ذخیره سازی داده`) _
  `هستن`
  )

(p
  `و به این چند مورد دسته بندی میشن:`)

(ul
  `..`
  `..`
  `..`
  )

(hr)
(ul
  (ref :db/concepts/norm/redundancy `افزونگی داده`))
]