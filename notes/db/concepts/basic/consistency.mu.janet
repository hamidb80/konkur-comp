[
(p
  (b `سازگاری`) _ 
  `یا` _ 
  (b `Consistency`) _ 
  `به طور خلاصه یعنی داده` _
  `(در ستون های همان جدول یا جداول دیگر)` _ 
  `باهم هماهنگ باشند`
  )

(p
  `بحث` _ 
  `consistency` _ 
  `در پایکاه داده رابطه ای، ناموسی ترین بحث است` _ 
  (i (b `یعنی اگر این مورد برقرار نباشد، اصلا پایگاه داده مفت نمی ارزد`))
  )
]