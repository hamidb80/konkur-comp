[

(p 
  `به یک زیر مجموعه از `
  :db/terms/attr _ 
  `های یک` _
  :db/terms/relation
  `،` _
  (b `کلید`) _
  `یا` _
  (b `key`) _
  `می گویند`
  )

(p `انواع کلیدها در پایگاه داده:`)
(ul
  (ref :db/concepts/keys/super-key `ابر کلید`)
  (ref :db/concepts/keys/candidate-key `کلید کاندید`)
  (ref :db/concepts/keys/primary-key `کلید اصلی`)
  (ref :db/concepts/keys/foreign-key `کلید خارحی`)
)
]