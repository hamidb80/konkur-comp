[
(title `تعریف ابرکلید`)

(p 
  :db/terms/key
  `ی که بتواند هر` _
  :db/terms/tuple _
  `از` _ 
  :db/terms/relation _ 
  ` را به صورت` _
  (b `یکتا`) _
  `،شناسایی کند` _ 
  (b `ابرکلید`) _
  `یا` _
  (b `super key`) _
  `است`
  )

(p
  `توجه:` _
  `بنابر ویژگی یکتا بودن، ابر کلید باید حتما شامل` _
  (ref :db/concepts/keys/candidate-key `کلید کاندید`) _
  `باشد`)
]