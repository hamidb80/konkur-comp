[
(p
   `تعریف` _
   (b `ابرکلید`) _
   `یا` _
   (b `super key`) _
   `:`)

#   :db/terms/key

(p 
  :db/terms/key
  `ی که بتواند هر` _
  :db/terms/tuple _
  `از رابطه را به صورت` _
  (b `یکتا`) _
  ` شناسایی کند`)

(p
  `توجه:` _
  `بنابر ویژگی یکتا بودن، ابر کلید باید حتما شامل` _
  (ref :db/concepts/keys/candidate-key `کلید کاندید`) _
  `باشد`)
]