[

(p
  (b `کلید کاندید`) _
  `یا` _
  (b `candidate key`) _
  `،` _
  :db/terms/key
  `ی` _ 
  `که بتوند به صورت یکتا هر` _ 
  :db/terms/tuple _
  `از` _ 
  :db/terms/relation _ 
  `را شناسایی کند و` _

  (ref :db/concepts/keys/minimal
    (b `کمینه`) _
    `یا` _
    (b `minimal`) _
  )
  ` باشد`_ 
  )

(p
  `نکته:` _
  `یک رابطه میتواند چندین کلید کاندید داشته باشد`)
]