[
(p
   `تعریف` _
   (b `کلید کاندید`) _
   `یا` _
   (b `candidate key`) _
   `:`)

(p
  (ref :db/concepts/keys/index `کلید`)
  `ی `
  `که بتوند به صورت یکتا هر`
  `tuple` _
  `رابطه را شناسایی کند و` _

  (ref :db/concepts/keys/minimal
    (b `کمینه`) _
    `یا` _
    (b `minimal`) _
  )
  ` باشد`_ 
  )

(p
  `نکته:` _
  `یک رابطه میتواند چندین کلید کاندید داشته باشد`)
]