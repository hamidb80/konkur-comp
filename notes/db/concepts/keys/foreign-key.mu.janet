[
(title `تعریف کلید حارجی`)

(p 
  (b `کلید خارحی`) _
  `یا` _
  (b `foreign key`) _

  `بیان میکند که` _ 
  :db/terms/attr _ 
  `مشخص شده از` _ 
  :db/terms/relation _ 
  (ltxi `A`) _ 
  `باید در رابطه` _ 
  (ltxi `B`) _ 
  :db/terms/primary-key _ 
  `باشد`
  )

]